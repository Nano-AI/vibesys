Profiler-guided kernel analysis is not wired up for the `cpu` backend — there is no torch/nsys profile to capture. Skip the profile-capture step and proceed to the Optimization opportunities section using diagnosis evidence only: the benchmark harness (`benchmark/benchmark.py`, which runs the compiled binary and reports `events_per_sec` on its `Primary metric:` line) plus source reading of the hot path (stream parse, window-state maintenance, snapshot emission). (TODO: a sampling CPU profiler — `perf` / `cargo flamegraph` — workflow could live here.)

Still classify the **primary bottleneck** in your analysis into exactly one of: `cpu_launch_bound`, `python_overhead_bound`, `input_pipeline_bound`, `sync_bound`, `memcpy_bound`, `comm_bound`, `kernel_bound`, `mixed_or_unclear` — using the diagnosis evidence above. State explicitly that the classification is profile-free.
