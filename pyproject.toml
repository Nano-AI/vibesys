[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "vibe-database"
version = "0.1.0"
description = "AI agent system for synthesizing bespoke streaming / database query engines"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "agentshim>=0.5.0",
    "deepagents",
    "jinja2>=3.1",
    "langchain-google-vertexai>=3.2.2",
    "langchain-openai",
    "openai>=1.28.0",
    "pydantic>=2",
    "python-dotenv>=1.0.1",
    "loguru>=0.7.2",
    "mcp>=1.0",
    "modal>=1.4.2",
]

[project.scripts]
vibe-database = "vibe_database.cli:main"
vibe-database-issue-mcp = "vibe_database.loops.plain.mcp_server:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["vibe_database*"]

[tool.setuptools.package-data]
vibe_database = ["templates/*.j2", "templates/_backend/*/*.j2", "loops/agent/templates/*.j2", "loops/agent/templates/_domain/*.md", "loops/agent/templates/_modality/*/*.j2", "loops/evolve/templates/*.j2", "loops/plain/templates/*.j2", "loops/plain/templates/*/*.j2"]

[dependency-groups]
dev = [
    "matplotlib>=3.10.8",
    "pytest",
    "ruff>=0.6",
]
test = [
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "W"]
ignore = ["E501"]

[tool.uv]
package = true

[tool.uv.workspace]
exclude = ["exp_env"]
members = []

[tool.uv.sources]
agentshim = { git = "https://github.com/vic-lsh/agentshim.git", rev = "codex/executor-injection" }
